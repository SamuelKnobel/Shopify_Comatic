services:
  - type: web
    name: shopify-comatic-bridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.app:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: DASHBOARD_USERNAME
        sync: false # Set this in Render Dashboard Environment settings
      - key: DASHBOARD_PASSWORD
        sync: false # Set this in Render Dashboard Environment settings
      - key: SQLITE_DB_PATH
        value: /var/data/sync.db
      - key: SHOPIFY_SHOP
        sync: false # You will set this in Render UI
      - key: SHOPIFY_CLIENT_ID
        sync: false
      - key: SHOPIFY_CLIENT_SECRET
        sync: false
      - key: COMATIC_PAYMENT_ACCOUNTS
        value: '{"Wise": "1001", "Bank Transfer": "1002"}'
